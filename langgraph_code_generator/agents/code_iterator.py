from .base_agent import BaseAgent

class CodeIteratorAgent(BaseAgent):
    # TODO: Implement the code iterator agent, leveraging the prompts/iterator.py file and the BASE_SCHEMA.xml file
    # The code iterator agent should take the generated code and tests for each file and iterate on the code until the tests pass and the code meets the planning requirements
    pass